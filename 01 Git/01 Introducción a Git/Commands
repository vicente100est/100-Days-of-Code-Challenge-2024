git init                                    = Inicia repositorio en git
git add biografia.tx                        = Agrega un archivo en especifico
git commit -m "version 1"                   = Hace un commit
git add .                                   = Agrega todos los archivos modificados
git status                                  = Lista todos los archivos que sufrieron un cambio
git show biografia.txt                      = Ver los cambios que han existido en un archivo
git log biografia.txt                       = Presenta todos los cambios que ha sufrido un archivo
git push                                    = Sube cambios a un repositorio remoto
esc + shift + z + z                         = Salir de la ventana de agregados
git diff 'id commit' 'comparar'             = Ver la diferencia de los commits
git checkout                                = Traer cambios que quiera para mi carpeta
git reset 'id de commit'                    = Regresar a un commit en especifico
git rm                                      = Elimina un archivo para no subir al add
git clone 'url'                             = Clona repositorio
git fetch                                   = Trae cambios de un repositorio remoto
git merge                                   = Junta los cambios ce otra rama a la rama donde se esta trabajando
git pull                                    = Lo mismo que un git fetch y git merge
git branch 'rama'                           = Crea una rama
git branch                                  = Ver todas las ramas
git remote origin url                       = Agregar repositorio remoto
git remote -v                               = Ver los repositorios remotos
git log --all --graph --decorate --oneline  = Muestra toda la informacion de log pero de forma mas comprimida
git push origin --tags                      = Dejas tags en el github
git show-branch                             = Muestra todas las ramas
git show-branch --all                       = Igual que show-branch pero con mas informacion
gitk                                        = Abre todas las historias en interface visual
git rebase 'rama'                           = Cambia la historia de donde comenzó la rama
git stash                                   = Guardar en memoria temporal
git stash list                              = Ver las listas de stash
git stash pop                               = Ver el stash guardado
git stash drop                              = Borrar stash
git clean -dry-run                          = Borra archivos basura o duplicados
git clean -f                                = Borra archivos menos carpetas y archivos de gitignore
git cherry-pick                             = Permite coger uno o varios commits de otra rama sin tener que hacer un merge completo
git reflog                                  = Recupera TODO
git reset --hard 'id de commit'             = Regresar a un commit en especifico TOTAL, regresa toda la historia
git commit -amend                           = Para agregar algo que no se agrego en el commit anterior
git grep 'palaba a buscar'                  = Te muestra donde se utiliza la palabra a buscar
git grep -n 'palabra a buscar'              = Te muestra axactamente donde se utiliza la palabra buscada
git grep -c 'palabra'                       = Cuenta las veces que usaste la palabra
git log -S 'Palabra a buscar'               = Busca una palabra en los commits
git shortlog                                = Cuantos commit ha hecho cada integrante del equipo de trabajo
git shortlog -sn --all                      = Muestra todos los commit incluso los eliminados
git shortlog -sn --all --no-merges          = Muestra todos los commit menos los merges
git config --global alias.stats 'comando    = Crea un nuevo comando o alias en git
    a guardar'
git blame 'archivo'                         = Para saber quien creo que cosa
git blame -c 'archivo'                      = Igual que gi blame pero identado
git coamdo --help                           = Como funciona el comando especificado
